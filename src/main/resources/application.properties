spring.application.name=msvc-items
server.port=8002

# Instancias de forma manual los puertos
# Se debe poner el mismo nombre que esta en @FeignClient al final
spring.cloud.discovery.client.simple.instances.msvc-products[0].uri=http://localhost:8001/api/v1/products   
# Ruta 2 (Servicio 2)
spring.cloud.discovery.client.simple.instances.msvc-products[1].uri=http://localhost:9001/api/v1/products
# Se puede tener más una instancia por cada puerto (Esta manera es la forma manual en comparación con Eureka)